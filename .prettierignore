# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
bun.lock
bun.lockb

# Build output / generated
.svelte-kit
build
dist
node_modules

# Generated registry payloads (written by `pnpm registry:build`)
static/r

# Read-only vendored upstream React source
.reference

# Orchestrator-owned
.port-logs
.port-state.json

# Spec Kit's machine-written feature pointer, rewritten on every component
.specify/feature.json

# Spec Kit artefacts: generated prose, not source. Requiring them to be prettier-clean makes the
# lint gate fail on document formatting rather than on code, which no amount of fixing the
# component would resolve.
specs
