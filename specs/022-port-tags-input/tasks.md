---
description: 'Task list for the Tags Input port'
---

# Tasks: Port Tags Input Component

**Input**: Design documents from `specs/022-port-tags-input/` (plan.md, spec.md, research.md, data-model.md, contracts/tags-input-api.md, quickstart.md)

**Tests**: MANDATORY (constitution Principle III / VII). Tests are written before the files they exercise and must fail for the right reason (missing modules, not misunderstood behaviour) before Phase 3 begins.

**User stories** (from spec.md): US1 = type and remove tags with the keyboard (P1), US2 = delete tags and clear the whole list by pointer (P1), US3 = validate, deduplicate, and cap tags as they're added (P2), US4 = edit an existing tag in place (P3).

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no unmet dependency)
- **[Story]**: Which user story (US1–US4) this task serves; omitted for Setup/Barrel/Verification tasks that serve all of them
- Every task names an exact repository-root-relative file path

## Phase order (per CLAUDE.md §11 / this command's requirements)

Setup → Tests → Core component files → Barrel and types → Demo route → Registry entry and docs polish → Verification

---

## Phase 1: Setup

- [X] T001 [P] Add a stub `registry:ui` entry named `tags-input` to `registry.json` (`name`, `type: "registry:ui"`, `title: "Tags Input"`, `description: "Display a list of tags in an input field with the ability to add, edit, and remove them."`, empty `registryDependencies: []`, `dependencies: []`, empty `files: []`) so the file exists as an anchor for T024 to fill in later.
- [X] T002 [P] Confirm the zero-new-dependency plan (plan.md Technical Context: `@lucide/svelte` for the `X` icon, `useDirection` reused from `src/lib/components/ui/direction-provider/index.ts`, `FormControlState` reused from `src/lib/components/ui/checkbox-group/checkbox-group.svelte.ts`) by inspecting `package.json` at the repository root and both source files; make no edits if confirmed.

**Checkpoint**: Setup complete — test authoring can begin.

---

## Phase 2: Tests (MANDATORY — write first, confirm they fail before Phase 4)

> T004–T012 add `describe` blocks to the same file, `src/lib/components/ui/tags-input/tags-input.test.ts`, and therefore run sequentially against each other (never `[P]`). T003 creates a separate file (`tags-input.test.svelte`) and has no dependency on T004–T012, so it can run in parallel with them; T009, T011, and T012 depend on T003 (they use the harness's controlled/form/bare-provider render modes).

- [X] T003 [P] [US1] [US2] [US3] [US4] Create the composition-test harness `src/lib/components/ui/tags-input/tags-input.test.svelte` per the project's harness convention (see `phone-input.test.svelte`, `checkbox-group.test.svelte`): a single component with a discriminated `mode` prop covering `'default'` (bare `Root` + items built from `bind:value` + `Input` + `Clear`), `'controlled'` (`bind:value` with an external `onValueChange` spy), `'function-binding'` (a `bind:value={() => v, (n) => {...}}` that can decline a write), `'form'` (wraps the parts in a plain `<form>` with `name`/`required` props), `'rtl'` (wraps the parts in a `dir="rtl"` ancestor), `'validation'` (wires `max`/`onValidate`/`onInvalid` props through), `'editable'` (sets `editable` on the root), and `'bare-input'` / `'bare-item'` / `'bare-item-text'` / `'bare-item-delete'` / `'bare-clear'` / `'bare-label'` (render each non-Root part with no `TagsInput.Root` ancestor, and `'bare-item-text'`/`'bare-item-delete'` also with a `Root` but no `Item` ancestor, for the provider-error tests). Not collected by Vitest and not listed in `registry.json`.
- [X] T004 [US1] In `src/lib/components/ui/tags-input/tags-input.test.ts`, write the roles/ARIA/accessible-attributes tests derived from `contracts/tags-input-api.md` §4–5: the root renders `data-slot="tags-input"` with `data-disabled`/`data-invalid`/`data-readonly` written `? '' : undefined` and the resolved `dir`; `TagsInput.Label` renders `id={labelId}` `for={inputId}` and `data-slot="tags-input-label"`, and while it is mounted the input carries `aria-labelledby={labelId}` (absent when no Label is mounted, per D-6); the input renders `id={inputId}`, `data-slot="tags-input-input"`, `data-invalid`, `aria-readonly`, `disabled`, `readonly`; each `TagsInput.Item` renders `data-slot="tags-input-item"`, `aria-labelledby={textId}`, `aria-current`, `aria-disabled`, `data-state="active|inactive"`, `data-highlighted`, `data-editing`, `data-editable`, `data-disabled`; `TagsInput.ItemText` renders `id={textId}` and `data-slot="tags-input-item-text"` and falls back to `displayValue(item.value)` with no children; `TagsInput.ItemDelete` renders `type="button"`, `tabindex={disabled ? undefined : -1}`, `aria-labelledby={textId}`, `aria-controls={itemId}`, `aria-current`, `data-slot="tags-input-item-delete"`, `data-state`, `data-disabled`, and falls back to the `X` icon from `@lucide/svelte` with no children; `TagsInput.Clear` renders `type="button"`, `aria-disabled`, `data-slot="tags-input-clear"`, `data-state="visible|invisible"`, `data-disabled`.
- [X] T005 [US1] In the same file, add the keyboard state-machine tests for `TagsInput.Input`'s `onkeydown`, porting `data-model.md`'s table and the upstream test file's assertions: with three tags and an empty, caret-at-0 input — `ArrowLeft` highlights the last tag, `ArrowLeft` again the middle, `ArrowLeft` from the first tag clears the highlight and resets the caret to `(0, 0)`; `Home`/`End` jump to the first/last tag; `Escape` clears any highlight and resets the caret to `(0, 0)`; `Backspace` from an empty, caret-at-0 input with no highlight highlights the last tag without removing it, and `Backspace`/`Delete` on a highlighted tag removes it and highlights the previous/adjacent enabled tag (or clears the highlight and focuses the input when none remains); typing a printable character while a tag is highlighted clears the highlight and the character reaches the input; `ArrowLeft`/`Backspace` have no effect when `selectionStart !== 0` (caret not at start) or when there are zero tags; `loop` wraps navigation from the last tag back to the first and vice versa, the default (`false`) stops at the boundary; pressing `Enter` with text in the input adds a tag and clears the input, with no tag left highlighted.
- [X] T006 [US1] In the same file, add the uncontrolled add/remove tests: an empty, uncontrolled root seeded by `defaultValue`; typing text and pressing `Enter` via `userEvent` appends a trimmed tag to the internally-tracked value and clears the input; two `Backspace` presses (highlight, then remove) delete the last tag and return focus to the input; `disabled` suppresses every interaction (typing has no effect on tag state, delete buttons are inert, the clear button is inert) and is reflected as `data-disabled` on the root, the input, every item, every delete button, and the clear button; `readOnly` allows focus/selection on the input but suppresses add/remove/clear/edit and is reflected as `data-readonly` on the root and `aria-readonly` on the input.
- [X] T007 [US1] [US4] In the same file, using the `'controlled'` and `'function-binding'` harness modes from T003, add the controlled-mode tests: passing `value` + `onValueChange` makes the parent authoritative — internal `Enter`/`Backspace`/delete-button/clear interactions call `onValueChange` with the next array but do not move the displayed tags until the parent updates `value`; a function binding that declines the write (returns without updating the bound state) leaves the rendered tag list unchanged after an add/remove attempt.
- [X] T008 [US1] [US4] In the same file, using the `'rtl'` harness mode from T003, add the RTL test: under `dir="rtl"`, `ArrowRight` (not `ArrowLeft`) highlights the last tag from an empty caret-at-0 input, and `ArrowLeft`/`ArrowRight` invert relative to the LTR case for every navigation assertion in T005 (moving toward the start vs. the end of the list), matching FR-018/spec Edge Cases; `Home`/`End`/`Backspace`/`Delete`/`Escape` behave identically to LTR.
- [X] T009 [US2] In the same file, using the `'default'` harness mode from T003, add the pointer-interaction tests: clicking a specific tag's `TagsInput.ItemDelete` removes only that tag regardless of highlight state and without first requiring focus to move to it; clicking `TagsInput.Clear` on a non-empty list removes every tag in one `onValueChange([])` call and moves focus to the text input; with an empty tag list and no `forceMount`, `TagsInput.Clear` is not present in the accessibility tree (`queryByRole` / `queryByText` returns `null`); with `forceMount`, the clear button remains present (and `data-disabled`) while the list is empty; a `TagsInput.Clear` rendered with a `child` snippet emits the caller's element carrying the same `data-slot`/`data-state`/`data-disabled` attributes instead of a `<button>`; clicking a tag (not its delete button) highlights it (`data-highlighted` present, `aria-current="true"`) without removing it; also assert FR-020: clicking the root's padding (not a tag, not the input) moves focus to the text input; blurring the whole component (`userEvent.tab()` to an outside button) clears `data-highlighted` from every item.
- [X] T010 [US3] In the same file, using the `'validation'` harness mode from T003, add the validation/dedupe/max tests per spec US3: with an existing tag `"a"`, attempting to add `"a"` again is rejected, `onInvalid` fires with `"a"`, the tag list is unchanged, and the root/input carry `data-invalid`; an `onValidate` returning `false` for a candidate rejects it the same way without adding it; with `max={2}` and two existing tags, a third attempt is rejected and `onInvalid` fires without needing `onValidate` involvement; a rejected add still clears the input the same way a successful one does, without adding the tag twice; a subsequent successful add clears `data-invalid`. Also add the `addOnPaste` test: pasting `"a, b, a, ollie"` (delimiter `","`) with `addOnPaste` enabled and an `onValidate` rejecting `"ollie"` splits on the delimiter, trims each candidate, deduplicates against both the pasted batch and the existing value, filters through `onValidate`, adds every surviving value in a single `onValueChange` call, fires `onInvalid` once per candidate already present in the value and **not** for the `onValidate`-rejected `"ollie"` (upstream's paste path filters invalid candidates silently — research R-08), leaves `data-invalid` unset on the root and the input (the paste path never touches the invalid flag), and never falls back to native paste insertion (assert the input's DOM value stays empty). Also add the over-max paste test: with `max={2}` and two existing tags, pasting a batch of three or more candidates is rejected in full — `onInvalid` fires once with the raw, untrimmed pasted text, `onValueChange` is not called, and nothing is added.
- [X] T011 [US3] In the same file, add the delimiter-on-type test (upstream "controlled value with delimiter input"): typing `"a,"` into the input commits `"a"` as a tag and leaves the input empty; typing `"a,b"` (delimiter not last) commits nothing and leaves the text in the input; a custom `delimiter=";"` commits on `";"` and not on `","`. Then add the `addOnTab` and `blurBehavior` tests: with `addOnTab` enabled and non-empty input text, `Tab` adds the text as a tag (through the same validation path as `Enter`) and prevents the default focus move; with `addOnTab` disabled (default), `Tab` moves focus normally and does not add a tag; `blurBehavior={undefined}` (default) leaves typed, uncommitted text in the input after blur; `blurBehavior="add"` commits the input text as a new tag on blur (subject to the same validation as `Enter`); `blurBehavior="clear"` discards the input text on blur without adding a tag.
- [X] T012 [US4] In the same file, using the `'editable'` harness mode from T003, add the in-place editing tests per spec US4: with `editable` set and a non-disabled tag, double-clicking it (or highlighting it and pressing `Enter`) replaces its text with a focused, content-selected inline field (`tags-input-item-edit.svelte`) pre-filled with the tag's current text; changing the text and pressing `Enter` commits the new value through the same duplicate/`onValidate`/`onInvalid` path as adding a tag, closes the field, and re-highlights the tag; pressing `Escape` in the field discards the edit, restores the original text, re-highlights the tag, and returns focus to the text input; with `editable` unset (default), double-clicking a tag does nothing (no edit field appears); a per-item `disabled` prop independently suppresses that tag's selection/editing/delete interaction while the rest of the list stays interactive (FR-010); `TagsInput.ItemDelete` renders nothing for the item currently in edit mode; with `displayValue={(v) => v.toUpperCase()}` and `editable`, committing an edit of `"kickflip"` to `"heelflip"` leaves `value` as the raw `["heelflip"]` while the tag renders `HEELFLIP`, and adding `"HEELFLIP"` is not treated as a duplicate of the raw value.
- [X] T013 [US2] [US3] [US4] In the same file, using the `'bare-input'`, `'bare-item'`, `'bare-item-text'`, `'bare-item-delete'`, and `'bare-clear'`/`'bare-label'` harness modes from T003, add the provider-error tests: rendering `TagsInput.Label`, `TagsInput.Input`, `TagsInput.Item`, `TagsInput.Clear` without an ancestor `TagsInput.Root` each throw an error naming both the part and `` `<TagsInput.Root>` `` (`expect(() => render(...)).toThrow(/within/)`); rendering `TagsInput.ItemText` or `TagsInput.ItemDelete` without an ancestor `TagsInput.Item` (even inside a `TagsInput.Root`) throws an error naming both the part and `` `<TagsInput.Item>` `` (FR-017).
- [X] T014 [US2] In the same file, using the `'form'` harness mode from T003, add the form-integration tests per `contracts/tags-input-api.md` §7: inside a `<form>` with `name="tags"`, a hidden, form-associated `<input type="text" data-slot="tags-input-form-input">` carries the comma-joined tag list and dispatches a native `input` event on every change; `new FormData(form).get('tags')` returns the joined list; a `required` tags input with zero tags fails native form validation and blocks submission (`form.reportValidity()` returns `false`), and passes once a tag is added; the hidden input reflects `disabled`/`required` from the root; outside a `<form>` ancestor, no hidden input is rendered.

**Checkpoint**: `pnpm run test:unit -- --run src/lib/components/ui/tags-input/tags-input.test.ts` fails (missing `./tags-input.svelte.js` / `./index.js`) — expected, proceed to implementation.

---

## Phase 3: Core component files

> T015 (state module) is the sole prerequisite for every part. T016 (Root) depends on T015. T017 (Label), T018 (Input), and T019 (Item) all depend on T016 (they read the root context) but not on each other — `[P]`. T020 (ItemText) and T022 (ItemDelete) both depend on T019 (the item context) but not on each other — `[P]`; T021 (ItemEdit, internal) is used only by T020, so it must land before or alongside T020 and is not `[P]` with it (same-feature coupling, though a different file). T023 (Clear) depends on T016 only — `[P]` with T017–T019. One task per exported subcomponent from the plan's Public API section: `Root` (T016), `Label` (T017), `Input` (T018), `Item` (T019), `ItemText` (T020), `ItemDelete` (T022), `Clear` (T023); `ItemEdit` (T021) is internal, per plan.md's Project Structure.

- [X] T015 Implement `src/lib/components/ui/tags-input/tags-input.svelte.ts`: the pure helpers `splitByDelimiter(text, delimiter): string[]` and `findAdjacentIndex({ current, count, direction, loop, isEnabled }): number | null` (data-model.md "Pure helpers"); the `TagsInputRootState` class per data-model.md `TagsInputRootState` — getter-function constructor props (`getValue`, `setValue`, `getOnValidate`, `getOnInvalid`, `getDisplayValue`, `getAddOnPaste`, `getAddOnTab`, `getDisabled`, `getEditable`, `getLoop`, `getReadOnly`, `getBlurBehavior`, `getDelimiter`, `getMax`, `getDir`, `id`) — never captured snapshots; `$state` fields `highlightedIndex`, `editingIndex`, `isInvalidInput`, `#disabledValues` (`Set<string>`), `#hasLabel`, `inputElement`; `$derived` fields `value`, `count`, the pass-throughs (`disabled`/`readOnly`/`editable`/`loop`/`addOnPaste`/`addOnTab`/`delimiter`/`max`/`dir`/`blurBehavior`), `inputId`/`labelId`, `labelledBy`; methods `displayValue`, `isEnabledIndex`, `registerItem`, `registerLabel`, `addItem(text, { viaPaste? })` exactly per research R-08: guard `disabled || readOnly` ⇒ return `false`; **paste branch** (`addOnPaste && viaPaste`) — `splitByDelimiter` ⇒ if `value.length + split.length > max && max > 0` call `onInvalid(rawPastedText)` and return `false`; call `onInvalid(v)` once per candidate already in `value`; dedupe against `value` and against the batch; filter through `onValidate` (no `onInvalid` for `onValidate` rejections on this path); return `false` if nothing survives, otherwise one single `setValue([...value, ...survivors])` and return `true`; this branch never touches `isInvalidInput`, `highlightedIndex` or `editingIndex`; **single branch** — `value.length >= max && max > 0` ⇒ `onInvalid(rawUntrimmedText)`, return `false` (no `isInvalidInput`); then `trim`; `onValidate` false ⇒ `isInvalidInput = true`, `onInvalid(trimmed)`, return `false`; duplicate ⇒ `isInvalidInput = true`, `onInvalid(trimmed)`, **return `true`** (so the Input clears without adding twice); otherwise append, clear `highlightedIndex`/`editingIndex`, `isInvalidInput = false`, return `true`, `updateItem(index, text)`, `removeItem(index)`, `leaveItem()`, `clear()`, `onInputKeydown(event)` (the full state machine from data-model.md, including the pre-pass gate on `selectionStart !== 0`, the `isArrowStart`/`isArrowEnd` `dir`-aware mapping, and every row of the keyboard table); the `TagsInputItemState` class per data-model.md `TagsInputItemState` — constructor `{ root, getValue, getDisabled, id }`, derived `value`/`index`/`isHighlighted`/`isEditing`/`disabled`/`displayValue`/`textId`/`dataState`, methods `select()`/`edit()`/`remove()`; the two `Symbol`-keyed contexts (`Symbol('tags-input')`, `Symbol('tags-input-item')`) with `setTagsInputContext`/`getTagsInputContext(consumerName)` and `setTagsInputItemContext`/`getTagsInputItemContext(consumerName)`, each throwing `` `<${consumerName}>` must be used within `<TagsInput.Root>`. `` (or `` `<TagsInput.Item>` ``) per data-model.md's Contexts table and FR-017; export a `TagsInputBlurBehavior` type (`'add' | 'clear' | undefined`). This must make T005's and T015-scoped assertions in T004–T014 pass once wired through the parts below — no rune usage outside class fields is required here, but this file itself uses `$state`/`$derived` (a `.svelte.ts` module).
- [X] T016 Implement `src/lib/components/ui/tags-input/tags-input.svelte` (Root): module-script `TagsInputRootProps` (alias `TagsInputProps`) type per `contracts/tags-input-api.md` §3 and plan.md's Public API table, with every prop's JSDoc (including `@default`) copied from the upstream source/MDX; instance script destructuring `$props()` once (`ref = $bindable(null)`, `value = $bindable()`, `defaultValue = []`, `onValueChange`, `onValidate`, `onInvalid`, `displayValue = (value) => value.toString()`, `addOnPaste = false`, `addOnTab = false`, `disabled = false`, `editable = false`, `loop = false`, `blurBehavior`, `delimiter = ','`, `max = Number.POSITIVE_INFINITY`, `required = false`, `readOnly = false`, `name`, `dir`, `id = $props.id()`, `class: className`, `children`, `...restProps`); `value ??= untrack(() => defaultValue)`; resolves `dir` via `useDirection` from `$lib/components/ui/direction-provider/index.js` (prop → provider → DOM `[dir]` → `'ltr'`); instantiates `new TagsInputRootState({...})` and calls `setTagsInputContext(state)`; composes `FormControlState` from `$lib/components/ui/checkbox-group/checkbox-group.svelte.ts` and renders the clipped `<input type="text" data-slot="tags-input-form-input">` carrying `name`/`required`/`disabled`/the comma-joined value, with the bubbling `input`-event dispatch effect (D-7), only when a form ancestor is detected; renders `data-slot="tags-input"` with `data-disabled`/`data-invalid`/`data-readonly` (`? '' : undefined`) and `dir`, default classes translated from the upstream registry component's `zinc-*` styling to semantic tokens per CLAUDE.md §6; `{@render children?.()}`; `onclick`/`onmousedown`/`onblur` per research R-16 — `getIsClickedInEmptyRoot(target)` = `!target.closest('[data-slot="tags-input-item"]') && target.tagName !== 'INPUT'`; `onclick` focuses `state.inputElement`, `onmousedown` calls `event.preventDefault()`, `onblur` sets `state.highlightedIndex = null` when `event.relatedTarget` is neither `state.inputElement` nor contained by `ref`; each composes the caller's handler first and honours `defaultPrevented`; spreads `...restProps` and merges `class={cn(..., className)}` last. Depends on T015.
- [X] T017 [P] Implement `src/lib/components/ui/tags-input/tags-input-label.svelte` (Label): module-script `TagsInputLabelProps` type (`WithElementRef<HTMLLabelAttributes, HTMLLabelElement>`, no component-specific props beyond `ref`/`children`); instance script consuming `getTagsInputContext('TagsInput.Label')`; an `$effect` calling `state.registerLabel()` on mount and its teardown on unmount; renders `id={state.labelId}` `for={state.inputId}`, `data-slot="tags-input-label"`, `data-disabled` from the root's `disabled`. Depends on T016.
- [X] T018 [P] Implement `src/lib/components/ui/tags-input/tags-input-input.svelte` (Input): module-script `TagsInputInputProps` type (`WithElementRef<Omit<HTMLInputAttributes, 'value' | 'type' | 'disabled' | 'readonly' | 'dir'>, HTMLInputElement>`, no component-specific props); instance script consuming `getTagsInputContext('TagsInput.Input')`; renders `id={state.inputId}`, `type="text"`, `autocapitalize="off"`, `autocomplete="off"`, `autocorrect="off"`, `spellcheck="false"`, `aria-labelledby={state.labelledBy}`, `aria-readonly={state.readOnly}`, `disabled={state.disabled}`, `readonly={state.readOnly}`, `dir={state.dir}`, `data-slot="tags-input-input"`, `data-invalid`; DOM state as an uncontrolled `<input>` (no `value` binding); binds `bind:this` into `state.inputElement`; `oninput` handler implementing delimiter-on-type commit exactly as upstream's `onChange`: return early while `state.readOnly`; call the caller's `oninput` first and honour `defaultPrevented`; then, only when `state.delimiter === target.value.slice(-1)`, take `target.value.slice(0, -1)`, clear `target.value`, and — if that text is non-empty — commit it via `state.addItem(text)` and set `state.highlightedIndex = null`; `onkeydown` composing the caller's handler first, honouring `defaultPrevented`, then: the `Enter` branch (upstream `onCustomKeydown`) — when the input's DOM value is non-empty, call `state.addItem(value)`, clear `event.currentTarget.value` and set `state.highlightedIndex = null` on a truthy return, and `event.preventDefault()` in either case; the `Tab`-with-`addOnTab` branch (same `onCustomKeydown` path, only when `state.addOnTab`); then `state.onInputKeydown(event)`; then the "typing a printable character (`event.key.length === 1`) clears the highlight" branch (data-model.md); `onpaste` reading `event.clipboardData.getData('text')`, calling `event.preventDefault()`, then `state.addItem(text, { viaPaste: true })`, then setting `state.highlightedIndex = null`, only when `state.addOnPaste` is true (all splitting/dedup/validation stays inside `addItem`, per research R-08 — this handler does no parsing of its own); `onblur` implementing `state.blurBehavior` (`'add'` commits via `addItem`, `'clear'` resets the DOM value, unset leaves it). Depends on T016.
- [X] T019 [P] Implement `src/lib/components/ui/tags-input/tags-input-item.svelte` (Item): module-script `TagsInputItemProps` type (`WithElementRef<HTMLAttributes<HTMLDivElement>, HTMLDivElement>` + required `value: string`, `disabled? = false`, `children?: Snippet`); instance script consuming `getTagsInputContext('TagsInput.Item')`, instantiating `new TagsInputItemState({ root: rootState, getValue: () => value, getDisabled: () => disabled, id: $props.id() })` and calling `setTagsInputItemContext(itemState)`; an `$effect` calling `rootState.registerItem(() => value, () => disabled)` on mount and its teardown on unmount; renders an ARIA object spread (`aria-labelledby={itemState.textId}`, `aria-current={itemState.isHighlighted}`, `aria-disabled={itemState.disabled}`) exactly as `checkbox-group.svelte` does; `ondblclick` calling `itemState.edit()` (no-op unless `editable` and not disabled); `onpointerup` calling `itemState.select()` when not disabled (unifies upstream's touch/pen `onclick` and mouse `onpointerup` into a single pointer-type-agnostic handler, since Svelte pointer events already normalise the three input types — spec Assumptions divergence); `onpointermove`/`onpointerleave` calling `rootState.leaveItem()` when this item is `disabled`, or when it currently holds the highlight and the pointer leaves it (upstream `onItemLeave`); `data-slot="tags-input-item"`, `data-state={itemState.dataState}`, `data-highlighted`, `data-editing`, `data-editable` (root's `editable`), `data-disabled`. Depends on T016.
- [X] T020 Implement `src/lib/components/ui/tags-input/tags-input-item-text.svelte` (ItemText): module-script `TagsInputItemTextProps` type (`WithElementRef<HTMLAttributes<HTMLSpanElement>, HTMLSpanElement>`, `children?: Snippet`); instance script consuming `getTagsInputItemContext('TagsInput.ItemText')`; while `itemState.isEditing && rootState.editable && !itemState.disabled` renders `tags-input-item-edit.svelte` instead of the span; otherwise renders `id={itemState.textId}`, `data-slot="tags-input-item-text"`, and `{@render children?.() ?? itemState.displayValue}` (falls back to the item's `displayValue`). Depends on T019 and T021.
- [X] T021 [P] Implement `src/lib/components/ui/tags-input/tags-input-item-edit.svelte` (internal, not exported from the barrel — upstream's `TagsInputEditableItemText`): consumes `getTagsInputItemContext('TagsInput.ItemText')` (it is only ever instantiated by T020, inside an Item, so it reuses the same context error name); local `editValue = $state(itemState.displayValue)` that re-initialises on every mount (per plan.md's Public API "Internal" note — a fresh instance each time editing starts); renders `data-slot="tags-input-item-edit"`, `aria-describedby={itemState.textId}`; an `$effect` that focuses the field and selects its content on mount; `onkeydown`: `Enter` calls `rootState.updateItem(itemState.index, editValue)` and `preventDefault()`s; `Escape` discards the edit without committing — sets `rootState.editingIndex = null` and `rootState.highlightedIndex = itemState.index` to restore the original text and re-highlight the tag — and `preventDefault()`s; `onblur` discards the edit the same way `Escape` does (data-model.md "Editing index"). Depends on T019.
- [X] T022 [P] Implement `src/lib/components/ui/tags-input/tags-input-item-delete.svelte` (ItemDelete): module-script `TagsInputItemDeleteProps` type (`WithElementRef<Omit<HTMLButtonAttributes, 'type'>, HTMLButtonElement>`, `children?: Snippet`); instance script consuming `getTagsInputItemContext('TagsInput.ItemDelete')`; renders nothing (`{#if !itemState.isEditing}`) while the item is editing; otherwise `type="button"`, `tabindex={itemState.disabled ? undefined : -1}`, `aria-labelledby={itemState.textId}`, `aria-controls={itemState.id}`, `aria-current={itemState.isHighlighted}`, `data-slot="tags-input-item-delete"`, `data-state={itemState.dataState}`, `data-disabled`; `onclick` calling `itemState.remove()`; `{@render children?.() ?? defaultXIcon}` — falls back to the `X` icon from `@lucide/svelte`. Depends on T019.
- [X] T023 [P] Implement `src/lib/components/ui/tags-input/tags-input-clear.svelte` (Clear): module-script `TagsInputClearProps` and `TagsInputClearChildProps` types (`WithElementRef<Omit<HTMLButtonAttributes, 'type'>, HTMLButtonElement>` + `forceMount? = false`, `child?: Snippet<[{ props: TagsInputClearChildProps }]>`, `children?: Snippet`); instance script consuming `getTagsInputContext('TagsInput.Clear')`; renders only while `forceMount || rootState.count > 0`; `type="button"`, `aria-disabled={rootState.disabled}`, `data-slot="tags-input-clear"`, `data-state={rootState.count > 0 ? 'visible' : 'invisible'}`, `data-disabled`; `onclick` calling `rootState.clear()` when not disabled; `{#if child}{@render child({ props })}{:else}{@render children?.()}{/if}` per CLAUDE.md §10's `asChild`/`child` translation. Depends on T016.

**Checkpoint**: engine + state + all part files exist; T004–T014 still fail only on the missing barrel (`./index.js`).

---

## Phase 4: Barrel and types

- [X] T024 Create `src/lib/components/ui/tags-input/index.ts`: imports `Root`/`Label`/`Input`/`Item`/`ItemText`/`ItemDelete`/`Clear` from their `.svelte` files; re-exports `TagsInputRootState`, `TagsInputItemState`, `setTagsInputContext`, `getTagsInputContext`, `setTagsInputItemContext`, `getTagsInputItemContext`, `splitByDelimiter`, `findAdjacentIndex`, and the types `TagsInputRootStateProps`, `TagsInputItemStateProps`, `TagsInputBlurBehavior` from `./tags-input.svelte.js`; re-exports `TagsInputRootProps`/`TagsInputProps` from `./tags-input.svelte`, `TagsInputLabelProps` from `./tags-input-label.svelte`, `TagsInputInputProps` from `./tags-input-input.svelte`, `TagsInputItemProps` from `./tags-input-item.svelte`, `TagsInputItemTextProps` from `./tags-input-item-text.svelte`, `TagsInputItemDeleteProps` from `./tags-input-item-delete.svelte`, `TagsInputClearProps`/`TagsInputClearChildProps` from `./tags-input-clear.svelte`; exports `Root`, `Label`, `Input`, `Item`, `ItemText`, `ItemDelete`, `Clear` plus the aliases `Root as TagsInput`, `Label as TagsInputLabel`, `Input as TagsInputInput`, `Item as TagsInputItem`, `ItemText as TagsInputItemText`, `ItemDelete as TagsInputItemDelete`, `Clear as TagsInputClear` — matching `contracts/tags-input-api.md` §1 exactly. Depends on T015–T023.

**Checkpoint**: run `pnpm run test:unit -- --run src/lib/components/ui/tags-input/tags-input.test.ts` — all of T004–T014's assertions should now pass. Fix `tags-input.svelte.ts`/the seven `.svelte` part files (never the tests) until they do.

---

## Phase 5: Demo route

- [X] T025 [US1] [US2] [US3] [US4] Create `src/routes/docs/components/tags-input/+page.svelte` with four `<ComponentPreview>` sections, one per upstream demo file under `.reference/diceui/docs/registry/bases/radix/examples/tags-input-*.tsx`: **Default** (label + wrapping tag list + `Add trick…` input + clear button, mirrors `tags-input-demo.tsx`), **Editable** (three seeded tags, `editable` + `addOnPaste`, `TagsInput.Clear` rendered as a `Button` from `$lib/components/ui/button/index.js` through the `child` snippet, mirrors `tags-input-editable-demo.tsx`), **With Validation** (`max={6}`, an `onValidate` rejecting values under 3 characters, `onInvalid` showing a `svelte-sonner` toast per rejection, mirrors `tags-input-validation-demo.tsx`), **With Sortable** (per spec Assumptions: reproduced with this project's own drag/reorder-capable primitive if one exists under `src/lib/components/ui/`, otherwise simple pointer up/down reordering composed directly with `TagsInput.Item`, mirrors `tags-input-sortable-demo.tsx`). Keep all demo state as page-local `$state`; no `+page.ts`. Also render one props table per part (`Root`, `Label`, `Input`, `Item`, `ItemText`, `ItemDelete`, `Clear`) sourced from `plan.md`'s Public API section (SC-005). Depends on T024.

---

## Phase 6: Registry entry and docs polish

- [X] T026 Replace the T001 stub in `registry.json` with the full `tags-input` entry per `contracts/tags-input-api.md` §8: `"registryDependencies": ["direction-provider", "checkbox-group"]`, `"dependencies": ["@lucide/svelte"]`, and `"files"` listing exactly `src/lib/components/ui/tags-input/index.ts`, `src/lib/components/ui/tags-input/tags-input.svelte`, `src/lib/components/ui/tags-input/tags-input-label.svelte`, `src/lib/components/ui/tags-input/tags-input-input.svelte`, `src/lib/components/ui/tags-input/tags-input-item.svelte`, `src/lib/components/ui/tags-input/tags-input-item-text.svelte`, `src/lib/components/ui/tags-input/tags-input-item-edit.svelte`, `src/lib/components/ui/tags-input/tags-input-item-delete.svelte`, `src/lib/components/ui/tags-input/tags-input-clear.svelte`, `src/lib/components/ui/tags-input/tags-input.svelte.ts` (both test files and the test harness excluded), each `"type": "registry:ui"`. Depends on T024.
- [X] T027 [P] Run `pnpm run registry:build` and confirm `static/r/tags-input.json` is generated with ten files and `$lib/...` imports rewritten to registry placeholders (`node -e "const r=require('./static/r/tags-input.json'); console.log(r.name, r.files.length, r.registryDependencies, r.dependencies)"` should print `tags-input 10 [ 'direction-provider', 'checkbox-group' ] [ '@lucide/svelte' ]`). Depends on T026.
- [X] T028 Docs polish: verify every `<ComponentPreview>` added in T025 has a `description` naming its source upstream demo file, and that the seven props tables match `plan.md`'s Public API section exactly (prop names, types, defaults, bindability). Depends on T025.

---

## Phase 7: Verification (MANDATORY — Principle VII, final phase)

- [X] T029 Run `pnpm run format`, then `pnpm run check`, `pnpm run lint`, `pnpm run test:unit -- --run` and `pnpm run build`, and fix everything that fails without suppressing anything.

---

## Dependencies & Execution Order

### Phase dependencies

- **Setup (Phase 1)**: no dependencies — T001 and T002 run in parallel.
- **Tests (Phase 2)**: depends on Setup. T003 is independent of T004–T014 (different file) and may run in parallel with them; T004–T014 are strictly sequential (same file, `tags-input.test.ts`); T007, T008, T009, T010, T012, T013, and T014 depend on T003 (they use the harness's `'controlled'`/`'function-binding'`, `'rtl'`, `'default'`, `'validation'`, `'editable'`, `'bare-*'`, and `'form'` modes) and on T004–T006 having already established the file.
- **Core component files (Phase 3)**: depends on Tests existing (so they can be observed failing). T015 is the sole prerequisite for all parts; T016 (Root) depends on T015; T017 (Label), T018 (Input), T019 (Item), and T023 (Clear) all depend on T016 but not on each other — parallel; T020 (ItemText) and T022 (ItemDelete) depend on T019; T021 (ItemEdit) depends on T019 and is used by T020.
- **Barrel and types (Phase 4)**: T024 depends on T015–T023.
- **Demo route (Phase 5)**: T025 depends on T024.
- **Registry entry and docs polish (Phase 6)**: T026 depends on T024; T027 depends on T026; T028 depends on T025. T027 and T028 may run in parallel with each other.
- **Verification (Phase 7)**: T029 depends on every prior task.

### Behavioural-area → task map (for traceability against CLAUDE.md §7)

| Area                                    | Task       |
| ---------------------------------------- | ---------- |
| Roles/ARIA + accessible names            | T004       |
| Keyboard interaction                     | T005       |
| Controlled vs uncontrolled               | T006, T007 |
| RTL                                      | T008       |
| Guard rails / edge cases                 | T006, T013 |
| Pointer interaction (delete/clear)       | T009       |
| Validation / dedupe / max / addOnPaste   | T010       |
| addOnTab / blurBehavior                  | T011       |
| In-place editing                         | T012       |
| Provider (throwing contexts)             | T013       |
| Form integration (hidden input)          | T014       |

---

## Parallel Example

```bash
# Setup — run together:
Task: "Add registry.json stub entry (T001)"
Task: "Confirm no new npm dependency is required (T002)"

# Tests — T003 runs alongside the start of the sequential T004..T014 chain:
Task: "Create tags-input.test.svelte harness (T003)"
Task: "Write roles/ARIA tests in tags-input.test.ts (T004)"

# Core component files — T017, T018, T019, T023 once T016 lands:
Task: "Implement tags-input-label.svelte (T017)"
Task: "Implement tags-input-input.svelte (T018)"
Task: "Implement tags-input-item.svelte (T019)"
Task: "Implement tags-input-clear.svelte (T023)"

# Registry/docs polish — run together once T026/T025 land:
Task: "Run pnpm run registry:build (T027)"
Task: "Verify demo descriptions and props tables (T028)"
```

---

## Implementation Strategy

1. Complete Phase 1 (Setup).
2. Complete Phase 2 (Tests) — confirm the suite fails only on missing imports, not on assertion logic errors that indicate a misunderstood spec.
3. Complete Phase 3 (Core component files) in order: state module → Root, then Label/Input/Item/Clear in parallel, then ItemText/ItemEdit/ItemDelete.
4. Complete Phase 4 (Barrel) — re-run the full test file; every scenario from T004–T014 should now pass.
5. Complete Phase 5 (Demo route) and Phase 6 (Registry + docs polish).
6. Complete Phase 7 (Verification) — all four gates green, zero suppressions, then `pnpm run registry:build` output committed.

Do NOT run git write commands — the orchestrator owns the working tree (Principle X). Do not touch `.reference/`, `.specify/`, `.claude/`, `scripts/`, `.port-state.json`, or `.port-logs/`.

---

## Phase 8: Convergence

> Appended by `/speckit-converge` after Phase 7 completed with all four gates green (1723 unit tests passing, 0 `svelte-check` errors, prettier + eslint clean, `build` succeeding). These are the residual gaps between the implemented port and spec.md / plan.md / the upstream sources; every other requirement, acceptance scenario, edge case, divergence and registry obligation was verified as satisfied. Re-run the Phase 7 gates after completing these.

- [X] T030 Suppress in-place editing while `readOnly` per FR-014 (partial): `TagsInputItemState.edit()` in `src/lib/components/ui/tags-input/tags-input.svelte.ts` gates only on `root.editable` and `this.disabled`, so a root with both `readOnly` and `editable` still opens the inline edit field on double-click (`tags-input-item.svelte`'s `ondblclick` → `item.edit()` → `tags-input-item-text.svelte` swaps in `tags-input-item-edit.svelte`); only the later `updateItem` commit is blocked. Add the `readOnly` guard to `edit()` and to the `Enter` edit branch of `TagsInputRootState.onInputKeydown` (which currently checks `editable && !disabled` only), so the guard holds even if a caller reaches the root state directly. Extend the `'keeps the input focusable but inert while readOnly'` test in `src/lib/components/ui/tags-input/tags-input.test.ts` (or add a sibling case) to render `readOnly` **and** `editable`, double-click a tag, and assert no `[data-slot="tags-input-item-edit"]` appears and the item carries no `data-editing`.
- [X] T031 Make blurring the inline edit field re-highlight its tag per FR-009 and task T021 (partial): `onblur` in `src/lib/components/ui/tags-input/tags-input-item-edit.svelte` clears `root.editingIndex` only, while FR-009 requires blur — like `Escape` — to discard the edit, restore the original text **and** return the tag to a highlighted (not editing) state; T021 specifies "`onblur` discards the edit the same way `Escape` does". Set `root.highlightedIndex = itemState.index` alongside `root.editingIndex = null`, without calling `inputElement.focus()` (blur must not steal focus back from wherever it just went — that part of the existing comment stays correct). Add the missing test to the `TagsInput editing (T012)` block in `src/lib/components/ui/tags-input/tags-input.test.ts`: open an edit field, type a change, blur it (`userEvent.tab()` or clicking the text input), then assert `onValueChange` was not called, the tag still renders its original text, `[data-slot="tags-input-item-edit"]` is gone, and the item carries `data-highlighted` — the blur-discard path currently has no assertion anywhere (SC-004).
- [X] T032 Render the hidden form-associated input whenever a `<form>` ancestor exists, regardless of `name`, per FR-016 (partial): `src/lib/components/ui/tags-input/tags-input.svelte` guards it with `{#if formControl.isFormControl && name}`, so a `required` tags input with no `name` renders no form control at all and native `required` validation cannot block submission — which is the exact reachability divergence D-7 exists to guarantee, and `contracts/tags-input-api.md` §7 conditions the input only on being inside a `<form>`. Change the guard to `{#if formControl.isFormControl}` and emit `name` only when supplied (`{name}` already omits an `undefined` attribute). Add a case to the `TagsInput form integration (T014)` block in `src/lib/components/ui/tags-input/tags-input.test.ts` asserting that a `required` tags input with zero tags and **no** `name` still fails `form.reportValidity()`, and that the rendered hidden input carries no `name` attribute; keep the existing "no hidden input outside a form" assertion passing.
- [X] T033 [P] Replace the item's physical padding with its logical equivalent per FR-018 (partial): `src/lib/components/ui/tags-input/tags-input-item.svelte` copies upstream's `[&:not([data-editing])]:pr-1.5`, but under `dir="rtl"` the `<TagsInput.ItemDelete>` sits at the logical end (visually left), so the tightened padding lands on the wrong side of the tag. Use `[&:not([data-editing])]:pe-1.5` so the layout follows start/end flow rather than a fixed left-to-right assumption, and re-run `pnpm run format`.
- [X] T034 [P] Assert that a max-cap rejection leaves the invalid state untouched per FR-015 (missing): the `'rejects an add past max with the raw, untrimmed text'` case in `src/lib/components/ui/tags-input/tags-input.test.ts` checks `onInvalid`, `onValidate` and the unchanged tag list, but never asserts the absence of `data-invalid`. FR-003/FR-004/FR-015 single this out as deliberate upstream parity ("the cap is a capacity limit, not an input-validity failure"), so add `expect(screen.getByTestId('root')).not.toHaveAttribute('data-invalid')` and the same assertion on the text input to that case.
- [X] T035 [P] Assert the delete button's `tabindex` inversion on the disabled branch per FR-011 (missing): the accessibility block in `src/lib/components/ui/tags-input/tags-input.test.ts` only asserts `tabindex="-1"` for an enabled item, leaving the documented `tabindex={disabled ? undefined : -1}` half untested. Render a per-item-`disabled` tag (the harness's `disabledValues` mode already supports this) and assert its `<TagsInput.ItemDelete>` has **no** `tabindex` attribute while it still carries `data-disabled` and `data-state="inactive"`.

**Checkpoint**: re-run `pnpm run format` → `pnpm run check` → `pnpm run lint` → `pnpm run test:unit -- --run` → `pnpm run build`; `registry.json` and `static/r/` need no change (no file was added or removed).
