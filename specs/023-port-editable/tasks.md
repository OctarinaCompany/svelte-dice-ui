---
description: 'Task list for the Editable port'
---

# Tasks: Port Editable Component

**Input**: Design documents from `specs/023-port-editable/` (plan.md, spec.md, research.md, data-model.md, contracts/public-api.md, contracts/data-attributes.md, contracts/upstream-test-map.md, quickstart.md)

**Tests**: MANDATORY (constitution Principle III / VII). Tests are written before the files they exercise and must fail for the right reason (missing modules, not misunderstood behaviour) before Phase 3 begins.

**User stories** (from spec.md): US1 = edit text in place with the mouse (P1), US2 = edit text in place with the keyboard (P1), US3 = choose how editing starts and read the current state visually (P2), US4 = compose triggers, toolbar, and label around the field (P3).

## Format: `[ID] [P?] [Story] Description`

- **[P]**: Can run in parallel (different files, no unmet dependency)
- **[Story]**: Which user story (US1–US4) this task serves; omitted for Setup/Barrel/Verification tasks that serve all of them
- Every task names an exact repository-root-relative file path

## Phase order (per CLAUDE.md §11 / this command's requirements)

Setup → Tests → Core component files → Barrel and types → Demo route → Registry entry and docs polish → Verification

---

## Phase 1: Setup

- [X] T001 [P] Add a stub `registry:ui` entry named `editable` to `registry.json` (`name`, `type: "registry:ui"`, `title: "Editable"`, `description: "An accessible inline editable component for editing text content in place."`, `registryDependencies: []`, `dependencies: []`, empty `files: []`) so the file exists as an anchor for T027 to fill in later.
- [X] T002 [P] Confirm the zero-new-dependency plan (plan.md Technical Context: `useDirection` reused from `src/lib/components/ui/direction-provider/index.ts`, `FormControlState` reused via the barrel `$lib/components/ui/checkbox-group/index.js` (declared in `src/lib/components/ui/checkbox-group/checkbox-group.svelte.ts`), `@lucide/svelte` used only by the demo page) by inspecting `package.json` at the repository root and both source files; make no edits if confirmed.

**Checkpoint**: Setup complete — test authoring can begin.

---

## Phase 2: Tests (MANDATORY — write first, confirm they fail before Phase 4)

> T004–T014 add `describe` blocks to the same file, `src/lib/components/ui/editable/editable.test.ts`, and therefore run sequentially against each other (never `[P]`). T003 creates a separate file (`editable.test.svelte`) and has no dependency on T004–T014, so it can run in parallel with them; T007, T009, T011, T012, T013, and T014 depend on T003 (they use the harness's `with-trigger`, `with-form`, `child-buttons`, `with-direction-provider`, and `bare-*` render modes).

- [X] T003 [P] [US1] [US2] [US3] [US4] Create the composition-test harness `src/lib/components/ui/editable/editable.test.svelte` per `contracts/upstream-test-map.md` §4 and the project's harness convention (see `tags-input.test.svelte`, `phone-input.test.svelte`): a single component with a discriminated `mode` prop covering `'default'` (Label + Area(Preview, Input) + Toolbar(Cancel, Submit) — upstream's `renderEditable`), `'with-trigger'` (`default` plus an external `Editable.Trigger` outside the Area), `'with-form'` (`default` wrapped in a `<form>` with a native submit button), `'child-buttons'` (Trigger/Submit/Cancel rendered through the `child` snippet onto `$lib/components/ui/button/index.js`), `'with-direction-provider'` (`default` inside `<DirectionProvider dir="rtl">` from `$lib/components/ui/direction-provider/index.js`), plus binding sub-modes on `default` — `'value'` (`bind:value`), `'editing'` (`bind:editing`), `'function'` (an authoritative getter/setter pair that can decline a write) — and the eight `'bare-label'`/`'bare-area'`/`'bare-preview'`/`'bare-input'`/`'bare-trigger'`/`'bare-toolbar'`/`'bare-cancel'`/`'bare-submit'` modes (one non-root part with no `Editable.Root` ancestor, for the FR-018 throw tests). Not collected by Vitest and not listed in `registry.json`.
- [X] T004 [US1] [US3] [US4] In `src/lib/components/ui/editable/editable.test.ts`, write the roles/ARIA/accessible-attributes tests derived from `contracts/public-api.md` and `contracts/data-attributes.md` for upstream test #1 (`renders without crashing`): the root renders `data-slot="editable"` with no root-level state `data-*`; `Editable.Label` renders `id={labelId}`, `for={inputId}`, `data-slot="editable-label"`, `data-disabled`/`data-invalid`/`data-required` mirroring the root; `Editable.Area` renders `role="group"`, `dir`, `data-slot="editable-area"`, `data-disabled`, `data-editing`; `Editable.Preview` (not editing, not readOnly, empty value) renders `role="button"`, `tabindex="0"`, `aria-disabled="false"`, `data-slot="editable-preview"` and `data-empty=""`, and does NOT carry `data-disabled` or `data-readonly` (both are absent in this state; `data-readonly` is unreachable on the preview by construction because the preview never renders while `readOnly` — the positive `data-disabled` case is asserted in T010); `Editable.Toolbar` renders `role="toolbar"`, `aria-controls={rootId}`, `aria-orientation="horizontal"`, `data-slot="editable-toolbar"`, `data-orientation="horizontal"`, and — while not editing/readOnly — no `Editable.Cancel`/`Editable.Submit` is present; with no `id` prop supplied, the root element MUST carry a non-empty `id` and every `aria-controls` in the tree (toolbar, and — in the `with-trigger` mode — the trigger, plus cancel/submit while editing) MUST resolve to that exact element via `document.getElementById` (D-3), and no `Editable.Input` is present (`queryByRole('textbox')` is `null`).
- [X] T005 [US1] In the same file, add the mouse-driven edit/submit/cancel tests mapping upstream test #2 (`handles controlled state`) and #3 (`handles cancellation`) per `contracts/upstream-test-map.md` §1: clicking the preview enters edit mode (`onEdit` and `onEditingChange(true)` fire, the preview is removed and a `textbox` with the current value appears); typing new text into the input calls `onValueChange` per keystroke; clicking `Editable.Submit` commits the current input text (`onSubmit('New Value')`, `onEditingChange(false)`), the preview reappears showing the new value, and the input is gone; separately, typing new text then clicking `Editable.Cancel` calls `onCancel`, the preview reappears showing the ORIGINAL value (not the typed text), and no `textbox` remains.
- [X] T006 [US1] [US2] In the same file, add the uncontrolled seed/focus/select test (independent test for US1 + FR-005/SC-002 + `contracts/upstream-test-map.md` §2 "Entering edit mode focuses the input and selects its whole content"): render an uncontrolled root with `defaultValue="Initial Value"`, click the preview, and assert (via `vi.waitFor`, since focus/select happen on a scheduled animation frame per research R-03) that the input is the active element (`document.activeElement`) and its full text is selected (`selectionStart === 0 && selectionEnd === value.length`); also assert the uncontrolled `defaultEditing` seed — rendering with `defaultEditing` and no `editing` prop shows the input (not the preview) on first render, `onEditingChange` is not called during initialisation, and a subsequent submit still leaves edit mode (FR-002).
- [X] T007 [US1] [US2] In the same file, using the `'value'`/`'editing'`/`'function'` binding modes from T003, add the controlled-mode tests per FR-002/R-02: passing `value` + `onValueChange` (and separately `editing` + `onEditingChange`) makes the parent authoritative — internal submit/cancel/type interactions call the callback with the next value/editing state but do not move the rendered preview/input until the parent feeds the prop back in; a `bind:value`/`bind:editing` function binding that declines the write (returns without updating the bound state) leaves the rendered output unchanged after a submit/cancel attempt (FR-002, Edge Cases); separately, using the `'value'`/`'editing'` binding modes (a plain `bind:value`/`bind:editing`), assert the display DOES follow immediately after a submit/cancel — the write-through case — distinguishing it from the bare-prop and declining-function-binding cases above (FR-002, Edge Cases).
- [X] T008 [US2] In the same file, add the keyboard tests mapping upstream test #4 (`handles keyboard navigation`) plus FR-008/FR-009/D-1 and `contracts/upstream-test-map.md` §2 ("`Escape` returns focus to the preview / external trigger", "`onEnterKeyDown` runs before the preview's edit"): focusing the preview and pressing `Enter` enters edit mode identically to a click, regardless of `triggerMode` (FR-008); with `onEnterKeyDown` calling `event.preventDefault()`, pressing `Enter` on the focused preview does NOT enter edit mode; pressing `Enter` inside the input calls `onSubmit` with the current text and exits edit mode; typing a change and pressing `Escape` inside the input calls `onCancel`, reverts the value to what it was when edit mode was entered, exits edit mode, and (via `vi.waitFor`) returns focus to the preview element that had focus before edit mode started; with `onEscapeKeyDown` calling `event.preventDefault()`, pressing `Escape` does NOT cancel (input stays open, value unchanged) — mapping upstream test #15b (`handles escape key down callback`).
- [X] T009 [US2] [US4] In the same file, using the `'with-trigger'` mode from T003, add the trigger-focus-restore test extending D-1/FR-007: activate edit mode via the external `Editable.Trigger` (not the preview), press `Escape` in the input, and assert (via `vi.waitFor`) that focus returns to the trigger button, not the preview.
- [X] T010 [US3] In the same file, add the disabled/read-only/placeholder/maxLength/autosize/trigger-mode tests mapping upstream tests #5–#10 per `contracts/upstream-test-map.md` §1: `disabled` — the preview carries `data-disabled=""`, clicking it does not call `onEdit` and no `textbox` appears, and the label/area also carry `data-disabled`, and (using the `'default'` mode in edit state, or asserting the submit/cancel no-op) clicking submit/cancel while `disabled` does nothing; `readOnly` — no preview `role="button"` is rendered, a `textbox` is always present with the native `readonly` attribute, and typing into it does not call `onValueChange`; an empty value with a `placeholder` — the preview shows the placeholder text and carries `data-empty=""`, and the input's native `placeholder` attribute matches; `maxLength={10}` — the input carries `maxlength="10"` (D-2, beyond what upstream asserted); `autosize` — the input carries the width-auto class (`w-auto`) instead of the default `w-full`; trigger modes — `triggerMode="dblclick"` (default `'click'`): a single click on the preview does not enter edit mode, a double click does; `triggerMode="focus"`: focusing the preview (e.g. via `userEvent.tab()`) enters edit mode immediately; `triggerMode="click"` (explicit default case): a single click enters edit mode; additionally, with `autosize` set, define a non-zero `scrollWidth` on the rendered input (`Object.defineProperty(input, 'scrollWidth', { value: 120, configurable: true })`) and assert `input.style.width` is written both after the edit-start frame (`vi.waitFor`) and after typing a character, proving FR-011's recalculation on edit start and on every value change — not just the `w-auto` class.
- [X] T011 [US3] [US4] In the same file, using the `'with-direction-provider'` mode from T003, add the RTL test mapping upstream test #12 (`supports RTL direction`): with `dir="rtl"` passed directly to the root and, separately, inherited from an ancestor `<DirectionProvider dir="rtl">`, assert `Editable.Area` and `Editable.Input` both carry `dir="rtl"`.
- [X] T012 [US4] In the same file, using the `'default'`, `'with-trigger'`, and `'child-buttons'` modes from T003, add the composition tests mapping upstream test #11 (`handles external trigger`) plus FR-014/FR-015/FR-016/FR-018-adjacent behaviour: an `Editable.Trigger` rendered outside `Editable.Area` enters edit mode on activation (`onEdit` fires) exactly as the preview would; the trigger is absent from the accessibility tree once editing starts, and present (with `data-disabled`/`data-readonly` as applicable) when `forceMount` is set; `Editable.Toolbar` with `orientation="vertical"` carries `aria-orientation="vertical"` and the vertical layout class (FR-015); `Editable.Cancel`/`Editable.Submit` are absent from the accessibility tree while not editing and not `readOnly`, and both appear once editing starts, each performing its documented action; a `child` snippet on `Editable.Trigger`/`Editable.Cancel`/`Editable.Submit` renders the caller's `<Button>` element carrying the same `data-slot`/`data-disabled` attributes instead of a native `<button>` (D-6).
- [X] T013 [US1] [US2] [US4] In the same file, add the remaining upstream-mapped and CLAUDE.md §7 cases: invalid state (upstream #14) — `invalid` reflects `data-invalid=""` on the label and `aria-invalid="true"` on the input; required state (upstream, unnumbered) — `required` reflects `data-required=""` on the label and `required`/`aria-required="true"` on the input; blur submission (upstream #15) — typing a change then moving focus to an unrelated element (`await user.tab()` to an outside button) submits (`onSubmit('New Value')`) and exits edit mode; blur toward `Editable.Cancel` or `Editable.Trigger` (via `await user.tab()` landing on either, or a direct `fireEvent.blur(input, { relatedTarget: cancelOrTriggerEl })` per `contracts/upstream-test-map.md` §3's one documented `fireEvent` exception) does NOT submit; a `fireEvent.blur(input)` with `relatedTarget === null` still submits; submitting an unchanged value (input text equals the value edit mode started with) still calls `onSubmit` and exits edit mode (FR-006, Edge Cases); `onValueChange` does not fire when a submitted/typed value did not actually change (R-01 guard); toggling `readOnly` while mid-edit forces the always-shown inert input state (no preview reappears) per the spec Edge Cases; using the `'bare-*'` modes from T003, each of `Editable.Label`, `Editable.Area`, `Editable.Preview`, `Editable.Input`, `Editable.Trigger`, `Editable.Toolbar`, `Editable.Cancel`, `Editable.Submit` rendered with no `Editable.Root` ancestor throws an error naming the part and `` `<Editable.Root>` `` (`expect(() => render(...)).toThrow(/within/)`) (FR-018); every part carries its documented `data-slot` (cross-check against `contracts/data-attributes.md`).
- [X] T014 [US4] In the same file, using the `'with-form'` mode from T003, add the form-integration test mapping upstream test #13 (`handles form integration`) plus FR-017/D-5: inside a `<form>` with `name="field"`, a hidden, form-associated `<input type="text" data-slot="editable-form-input">` carries the current value, and a native submit handler observes it (`new FormData(form).get('field')` returns the current value); a `required` editable field with an empty value fails native form validation and blocks submission (`form.reportValidity()` returns `false`, reachable only because of D-5's `type="text"` choice), and passes once a non-empty value is submitted; the hidden input reflects `disabled` from the root; outside a `<form>` ancestor, no hidden input is rendered.

**Checkpoint**: `pnpm run test:unit -- --run src/lib/components/ui/editable/editable.test.ts` fails (missing `./editable.svelte.js` / `./index.js`) — expected, proceed to implementation.

---

## Phase 3: Core component files

> T015 (state module) is the sole prerequisite for every part. T016 (Root) depends on T015. T017 (Label), T018 (Area), T019 (Preview), T020 (Input), T021 (Trigger), T022 (Toolbar), and their siblings all depend on T016 (they read the root context) but not on each other where noted — `[P]`. One task per exported subcomponent from the plan's Public API section: `Root` (T016), `Label` (T017), `Area` (T018), `Preview` (T019), `Input` (T020), `Trigger` (T021), `Toolbar` (T022), `Cancel` (T023), `Submit` (T024).

- [X] T015 Implement `src/lib/components/ui/editable/editable.svelte.ts` per `data-model.md` §1: the `EditableRootState` class — getter-function constructor props (`getValue`, `setValue`, `getEditing`, `setEditing`, `getOnEdit`, `getOnSubmit`, `getOnCancel`, `getOnEnterKeyDown`, `getOnEscapeKeyDown`, `getTriggerMode`, `getAutosize`, `getMaxLength`, `getPlaceholder`, `getDisabled`, `getReadOnly`, `getRequired`, `getInvalid`, `getDir`, plain `id`) — never captured snapshots; `$state` fields `inputElement`, `previewElement`; private non-reactive fields `#restoreValue` (seeded from the initial value) and `#trigger`; `$derived` fields `value`, `editing`, `triggerMode`, `autosize`, `maxLength`, `placeholder`, `disabled`, `readOnly`, `required`, `invalid`, `dir`, `rootId`, `inputId` (`` `${id}-input` ``), `labelId` (`` `${id}-label` ``), `isEmpty`; methods `edit(trigger?: HTMLElement)`, `submit(next: string)`, `cancel()` (using `tick()` from `svelte` then focusing `#trigger` falling back to `previewElement` if still connected, per D-1), `setText(next: string)`, `autosizeElement(target: HTMLInputElement | HTMLTextAreaElement)` (R-13: textarea → height reset then `scrollHeight`px; else width reset then `scrollWidth + 4`px; no-op when `!autosize`), `isBlurCommitting(related: EventTarget | null)` (R-15: `false` when `related` is an `HTMLElement` inside `[data-slot="editable-trigger"]` or `[data-slot="editable-cancel"]`, else `true`) — with the `Object.is` equality guard on `setValue`/`setEditing` writes (only call the write callback and downstream `onValueChange`/`onEditingChange` on an actual change) per `data-model.md` §1.1 "Equality guard"; the `Symbol('editable')`-keyed context with `setEditableContext(state)` / `getEditableContext(consumerName: string)` throwing `` `${consumerName}` must be used within `<Editable.Root>`. ``; export the types `EditableRootStateProps`, `EditableTriggerMode` (`'click' | 'dblclick' | 'focus'`). This file uses `$state`/`$derived` (a `.svelte.ts` module).
- [X] T016 Implement `src/lib/components/ui/editable/editable.svelte` (Root) per `plan.md`'s Public API `Root` table: module-script `EditableRootProps` (alias `EditableProps`) type — `WithElementRef<Omit<HTMLAttributes<HTMLDivElement>, 'dir'>, HTMLDivElement>` plus `value`/`defaultValue`/`onValueChange`/`editing`/`defaultEditing`/`onEditingChange`/`onEdit`/`onSubmit`/`onCancel`/`onEnterKeyDown`/`onEscapeKeyDown`/`triggerMode`/`autosize`/`maxLength`/`placeholder`/`name`/`disabled`/`readOnly`/`required`/`invalid`/`dir`/`id`/`child`/`children`, with every prop's JSDoc (including `@default`) copied from the upstream source/MDX; instance script destructuring `$props()` once (`ref = $bindable(null)`, `value = $bindable()`, `defaultValue = ''`, `editing = $bindable()`, `defaultEditing = false`, `triggerMode = 'click'`, `autosize = false`, `disabled = false`, `readOnly = false`, `required = false`, `invalid = false`, `id = $props.id()`, `class: className`, `child`, `children`, `...restProps`); `value ??= untrack(() => defaultValue)`, `editing ??= untrack(() => defaultEditing)`; resolves `dir` via `useDirection` from `$lib/components/ui/direction-provider/index.js` (prop → provider → DOM `[dir]` → `'ltr'`); instantiates `new EditableRootState({...})` and calls `setEditableContext(state)`; composes `FormControlState` from `$lib/components/ui/checkbox-group/index.js` (the barrel, exactly as `src/lib/components/ui/tags-input/tags-input.svelte:121` does) and renders the clipped `<input type="text" data-slot="editable-form-input">` carrying `name`/`required`/`disabled`/`readonly`/the current value, only when a form ancestor is detected (D-5, R-09); renders `data-slot="editable"` with class `flex min-w-0 flex-col gap-2` merged with the caller's `className` via `cn()` (no root-level state `data-*` per `plan.md`); always renders `id={id ?? $props.id()}` (D-3); renders `{#if child}{@render child({ props })}{:else}<div {...props}>{@render children?.()}</div>{/if}` per CLAUDE.md §10 (the root's `child` payload type is deliberately not exported — contracts/public-api.md:66); spreads `...restProps`. Depends on T015.
- [X] T017 [P] Implement `src/lib/components/ui/editable/editable-label.svelte` (Label): module-script `EditableLabelProps`/`EditableLabelChildProps` types (`WithElementRef<HTMLLabelAttributes, HTMLLabelElement>`, `child?: Snippet<[{ props: EditableLabelChildProps }]>`, `children?: Snippet`); instance script consuming `getEditableContext('<Editable.Label>')`; renders `id={state.labelId}`, `for={state.inputId}`, `data-slot="editable-label"`, `data-disabled`/`data-invalid`/`data-required` mirroring the root (`? '' : undefined`); `{#if child}{@render child({ props })}{:else}<label {...props}>{@render children?.()}</label>{/if}` per CLAUDE.md §10. Depends on T016.
- [X] T018 [P] Implement `src/lib/components/ui/editable/editable-area.svelte` (Area): module-script `EditableAreaProps`/`EditableAreaChildProps` types (`WithElementRef<Omit<HTMLAttributes<HTMLDivElement>, 'dir'>, HTMLDivElement>` + `child`/`children`); instance script consuming `getEditableContext('<Editable.Area>')`; renders `role="group"`, `dir={state.dir}`, `data-slot="editable-area"`, `data-disabled`, `data-editing` (`? '' : undefined`); `child`/`children` snippet split as T017. Depends on T016.
- [X] T019 Implement `src/lib/components/ui/editable/editable-preview.svelte` (Preview): module-script `EditablePreviewProps`/`EditablePreviewChildProps` types (`WithElementRef<HTMLAttributes<HTMLDivElement>, HTMLDivElement>` + `child`/`children`); instance script consuming `getEditableContext('<Editable.Preview>')`; renders only while `!state.editing && !state.readOnly` (`{#if}`, genuinely removed per FR-005); binds `bind:this` into `state.previewElement`; renders `role="button"`, `tabindex={state.disabled || state.readOnly ? undefined : 0}`, `aria-disabled={state.disabled || state.readOnly}` (upstream `editable.tsx:478`), `data-slot="editable-preview"`, `data-empty={state.isEmpty ? '' : undefined}`, `data-disabled`, `data-readonly`; renders `{#if children}{@render children()}{:else}{state.value || state.placeholder}{/if}` so the upstream default content (`value || placeholder`, `editable.tsx:495`) shows when the caller supplies no `children` snippet; `onclick` composing the caller's handler, honouring `defaultPrevented`, then `state.edit(previewEl)` when `state.triggerMode === 'click'` and not disabled/readOnly; `ondblclick` same for `state.triggerMode === 'dblclick'`; `onfocus` same for `state.triggerMode === 'focus'`; `onkeydown` composing the caller's handler, honouring `defaultPrevented`, then on `Enter`: call the root's `onEnterKeyDown` (via `state`) and, unless it called `preventDefault()`, `state.edit(previewEl)` regardless of `triggerMode` (FR-008) — all guarded by not disabled/readOnly. Depends on T016.
- [X] T020 Implement `src/lib/components/ui/editable/editable-input.svelte` (Input): module-script `EditableInputProps`/`EditableInputChildProps` types (`WithElementRef<Omit<HTMLInputAttributes, 'value' | 'dir'>, HTMLInputElement>` + `maxLength?`/`disabled?`/`readOnly?`/`required?`/`child`/`children`); instance script consuming `getEditableContext('<Editable.Input>')`; renders only while `state.editing || state.readOnly` (FR-005); binds `bind:this` into `state.inputElement`; renders `id={state.inputId}`, `aria-labelledby={state.labelId}`, `aria-required={required ?? state.required}`, `aria-invalid={state.invalid}`, `dir={state.dir}`, `placeholder={state.placeholder}`, `value={state.value}`, `data-slot="editable-input"`, `disabled={disabled ?? state.disabled}`, `readonly={readOnly ?? state.readOnly}`, `required={required ?? state.required}`, `maxlength={maxLength ?? state.maxLength}` (D-2: own prop wins); default class switches `w-full` ↔ `w-auto` on `state.autosize` (FR-011); an `$effect.pre` on mount that, on the next animation frame (cancelled on teardown, R-03), focuses the input, selects its full text (`select()`), and calls `state.autosizeElement(el)`; `oninput` composing the caller's handler, honouring `defaultPrevented`, then (unless `state.readOnly`) `state.setText(target.value)` and `state.autosizeElement(target)`; `onkeydown` composing the caller's handler, honouring `defaultPrevented`, then on `Enter` (unless disabled/readOnly) `event.preventDefault()` + `state.submit(target.value)`; on `Escape`, call the root's `onEscapeKeyDown` and, unless it called `preventDefault()`, `event.preventDefault()` + `state.cancel()` (FR-009); `onblur` composing the caller's handler, honouring `defaultPrevented`, then (unless disabled/readOnly) submitting via `state.submit(target.value)` only when `state.isBlurCommitting(event.relatedTarget)` is true (R-15). Not `bind:value` (R-12 — DOM value stays uncontrolled between the state's `value` and native input events). Depends on T016.
- [X] T021 [P] Implement `src/lib/components/ui/editable/editable-trigger.svelte` (Trigger): module-script `EditableTriggerProps`/`EditableTriggerChildProps` types (`WithElementRef<Omit<HTMLButtonAttributes, 'type'>, HTMLButtonElement>` + `forceMount? = false`, `child`/`children`); instance script consuming `getEditableContext('<Editable.Trigger>')`; renders while `forceMount || (!state.editing && !state.readOnly)`; renders `type="button"`, `aria-controls={state.rootId}`, `aria-disabled={state.disabled || state.readOnly}` (upstream `editable.tsx:686`; reachable when `forceMount` keeps the trigger rendered while the root is `readOnly`), `data-slot="editable-trigger"`, `data-disabled`, `data-readonly`; `onclick` composing the caller's handler, honouring `defaultPrevented`, then `state.edit(triggerEl)` when `state.triggerMode` is `'click'` or `'focus'` (D-4) and not disabled/readOnly; `ondblclick` same for `state.triggerMode === 'dblclick'`. Depends on T016.
- [X] T022 [P] Implement `src/lib/components/ui/editable/editable-toolbar.svelte` (Toolbar): module-script `EditableToolbarProps`/`EditableToolbarChildProps` types (`WithElementRef<Omit<HTMLAttributes<HTMLDivElement>, 'dir'>, HTMLDivElement>` + `orientation?: EditableToolbarOrientation = 'horizontal'`, `child`/`children`); export `EditableToolbarOrientation` (`'horizontal' | 'vertical'`) from this module script per `data-model.md` §1.3; instance script consuming `getEditableContext('<Editable.Toolbar>')`; always rendered; renders `role="toolbar"`, `aria-controls={state.rootId}`, `aria-orientation={orientation}`, `dir={state.dir}`, `data-slot="editable-toolbar"`, `data-orientation={orientation}`; layout class `flex-col` when `orientation === 'vertical'` (FR-015). Depends on T016.
- [X] T023 [P] Implement `src/lib/components/ui/editable/editable-cancel.svelte` (Cancel): module-script `EditableCancelProps`/`EditableCancelChildProps` types (`WithElementRef<Omit<HTMLButtonAttributes, 'type'>, HTMLButtonElement>` + `child`/`children`); instance script consuming `getEditableContext('<Editable.Cancel>')`; renders while `state.editing || state.readOnly`; renders `type="button"`, `aria-controls={state.rootId}`, `data-slot="editable-cancel"`, `data-disabled`, `data-readonly`; `onclick` composing the caller's handler, honouring `defaultPrevented`, then `state.cancel()` when not disabled/readOnly. Depends on T016.
- [X] T024 [P] Implement `src/lib/components/ui/editable/editable-submit.svelte` (Submit): module-script `EditableSubmitProps`/`EditableSubmitChildProps` types (`WithElementRef<Omit<HTMLButtonAttributes, 'type'>, HTMLButtonElement>` + `child`/`children`); instance script consuming `getEditableContext('<Editable.Submit>')`; renders while `state.editing || state.readOnly`; renders `type="button"`, `aria-controls={state.rootId}`, `data-slot="editable-submit"`, `data-disabled`, `data-readonly`; `onclick` composing the caller's handler, honouring `defaultPrevented`, then `state.submit(state.inputElement?.value ?? state.value)` when not disabled/readOnly. Depends on T016.

**Checkpoint**: engine + state + all nine part files exist; T004–T014 still fail only on the missing barrel (`./index.js`).

---

## Phase 4: Barrel and types

- [X] T025 Create `src/lib/components/ui/editable/index.ts`: imports `Root`/`Label`/`Area`/`Preview`/`Input`/`Trigger`/`Toolbar`/`Cancel`/`Submit` from their `.svelte` files; re-exports `EditableRootState`, `setEditableContext`, `getEditableContext`, and the types `EditableRootStateProps`, `EditableTriggerMode`, `EditableToolbarOrientation` from `./editable.svelte.ts`; re-exports `EditableRootProps`/`EditableProps` from `./editable.svelte`, `EditableLabelProps`/`EditableLabelChildProps` from `./editable-label.svelte`, `EditableAreaProps`/`EditableAreaChildProps` from `./editable-area.svelte`, `EditablePreviewProps`/`EditablePreviewChildProps` from `./editable-preview.svelte`, `EditableInputProps`/`EditableInputChildProps` from `./editable-input.svelte`, `EditableTriggerProps`/`EditableTriggerChildProps` from `./editable-trigger.svelte`, `EditableToolbarProps`/`EditableToolbarChildProps` from `./editable-toolbar.svelte`, `EditableCancelProps`/`EditableCancelChildProps` from `./editable-cancel.svelte`, `EditableSubmitProps`/`EditableSubmitChildProps` from `./editable-submit.svelte`; exports `Root`, `Label`, `Area`, `Preview`, `Input`, `Trigger`, `Toolbar`, `Cancel`, `Submit` plus the aliases `Root as Editable`, `Label as EditableLabel`, `Area as EditableArea`, `Preview as EditablePreview`, `Input as EditableInput`, `Trigger as EditableTrigger`, `Toolbar as EditableToolbar`, `Cancel as EditableCancel`, `Submit as EditableSubmit` — matching `contracts/public-api.md` exactly. Depends on T015–T024.

**Checkpoint**: run `pnpm run test:unit -- --run src/lib/components/ui/editable/editable.test.ts` — all of T004–T014's assertions should now pass. Fix `editable.svelte.ts`/the nine `.svelte` part files (never the tests) until they do.

---

## Phase 5: Demo route

- [X] T026 [US1] [US2] [US3] [US4] Create `src/routes/docs/components/editable/+page.svelte` with five `<ComponentPreview>` sections, one per upstream demo file under `.reference/diceui/docs/registry/bases/radix/examples/editable-*.tsx` per `quickstart.md` §3: **Default** (label, area with preview/input, external trigger + toolbar composed via `child` snippets onto `$lib/components/ui/button/index.js`, mirrors `editable-demo.tsx`), **With Double Click** (`triggerMode="dblclick"`, mirrors `editable-double-click-demo.tsx`), **With Autosize** (`autosize`, mirrors `editable-autosize-demo.tsx`), **Todo List** (multiple `Editable.Root` instances in a list, each `onSubmit` writing back to page-local `$state`, an icon trigger from `@lucide/svelte`, mirrors `editable-todo-list-demo.tsx`), **With Form** (two fields inside a `<form>`, one `invalid`, a toolbar, plain `$state` validation per the spec Assumptions divergence from `react-hook-form`/`zod`, mirrors `editable-form-demo.tsx`). Keep all demo state as page-local `$state`; no `+page.ts`. Also render nine props tables (one per part) sourced from `plan.md`'s Public API section and a keyboard table (`Enter`/`Escape`/`Tab` per SC-005). Depends on T025.

---

## Phase 6: Registry entry and docs polish

- [X] T027 Replace the T001 stub in `registry.json` with the full `editable` entry per `plan.md`'s "Registry entry shape": `"registryDependencies": ["direction-provider", "checkbox-group"]`, `"dependencies": []`, and `"files"` listing exactly the 11 non-test files under `src/lib/components/ui/editable/` — `index.ts`, `editable.svelte`, `editable-label.svelte`, `editable-area.svelte`, `editable-preview.svelte`, `editable-input.svelte`, `editable-trigger.svelte`, `editable-toolbar.svelte`, `editable-cancel.svelte`, `editable-submit.svelte`, `editable.svelte.ts` (`editable.test.ts` and `editable.test.svelte` excluded), each `"type": "registry:ui"`. Depends on T025.
- [X] T028 [P] Run `pnpm run registry:build` and confirm `static/r/editable.json` is generated with 11 files and `$lib/...` imports rewritten to registry placeholders (`node -e "const r=require('./static/r/editable.json'); console.log(r.name, r.files.length, r.registryDependencies, r.dependencies)"` should print `editable 11 [ 'direction-provider', 'checkbox-group' ] []`). Depends on T027.
- [X] T029 Docs polish: verify every `<ComponentPreview>` added in T026 has a `description` naming its source upstream demo file, and that the nine props tables and the keyboard table match `plan.md`'s Public API section exactly (prop names, types, defaults, bindability). Depends on T026.

---

## Phase 7: Verification (MANDATORY — Principle VII, final phase)

- [X] T030 Run `pnpm run format`, then `pnpm run check`, `pnpm run lint`, `pnpm run test:unit -- --run` and `pnpm run build`, and fix everything that fails without suppressing anything.

---

## Dependencies & Execution Order

### Phase dependencies

- **Setup (Phase 1)**: no dependencies — T001 and T002 run in parallel.
- **Tests (Phase 2)**: depends on Setup. T003 is independent of T004–T014 (different file) and may run in parallel with them; T004–T014 are strictly sequential (same file, `editable.test.ts`); T007, T009, T011, T012, T013, and T014 depend on T003 (they use the harness's `'value'`/`'editing'`/`'function'`, `'with-trigger'`, `'with-direction-provider'`, `'child-buttons'`, and `'bare-*'`/`'with-form'` modes) and on T004–T006 having already established the file.
- **Core component files (Phase 3)**: depends on Tests existing (so they can be observed failing). T015 is the sole prerequisite for all parts; T016 (Root) depends on T015; T017 (Label), T018 (Area), T019 (Preview), T020 (Input), T021 (Trigger), T022 (Toolbar), T023 (Cancel), and T024 (Submit) all depend on T016 but not on each other — parallel, except T019 (Preview) and T020 (Input) are marked without `[P]` against each other since both read/write `state.inputElement`/`state.previewElement` and are easiest to get right implemented together.
- **Barrel and types (Phase 4)**: T025 depends on T015–T024.
- **Demo route (Phase 5)**: T026 depends on T025.
- **Registry entry and docs polish (Phase 6)**: T027 depends on T025; T028 depends on T027; T029 depends on T026. T028 and T029 may run in parallel with each other.
- **Verification (Phase 7)**: T030 depends on every prior task.

### Behavioural-area → task map (for traceability against CLAUDE.md §7)

| Area                                      | Task              |
| ------------------------------------------ | ----------------- |
| Roles/ARIA + accessible names              | T004               |
| Keyboard interaction (Enter/Escape/Tab)    | T006, T008, T009   |
| Controlled vs uncontrolled                 | T006, T007         |
| RTL                                        | T011               |
| Guard rails / edge cases                   | T010, T013         |
| Composition (trigger/toolbar/label/child)  | T012               |
| Provider (throwing context)                | T013               |
| Form integration (hidden input)            | T014               |

---

## Parallel Example

```bash
# Setup — run together:
Task: "Add registry.json stub entry (T001)"
Task: "Confirm no new npm dependency is required (T002)"

# Tests — T003 runs alongside the start of the sequential T004..T014 chain:
Task: "Create editable.test.svelte harness (T003)"
Task: "Write roles/ARIA tests in editable.test.ts (T004)"

# Core component files — T017, T018, T021, T022, T023, T024 once T016 lands:
Task: "Implement editable-label.svelte (T017)"
Task: "Implement editable-area.svelte (T018)"
Task: "Implement editable-trigger.svelte (T021)"
Task: "Implement editable-toolbar.svelte (T022)"
Task: "Implement editable-cancel.svelte (T023)"
Task: "Implement editable-submit.svelte (T024)"

# Registry/docs polish — run together once T027/T026 land:
Task: "Run pnpm run registry:build (T028)"
Task: "Verify demo descriptions and props tables (T029)"
```

---

## Implementation Strategy

1. Complete Phase 1 (Setup).
2. Complete Phase 2 (Tests) — confirm the suite fails only on missing imports, not on assertion logic errors that indicate a misunderstood spec.
3. Complete Phase 3 (Core component files) in order: state module → Root, then Label/Area/Trigger/Toolbar/Cancel/Submit in parallel, then Preview/Input (the focus-management crux — implement and verify together).
4. Complete Phase 4 (Barrel) — re-run the full test file; every scenario from T004–T014 should now pass.
5. Complete Phase 5 (Demo route) and Phase 6 (Registry + docs polish).
6. Complete Phase 7 (Verification) — all four gates green, zero suppressions, then `pnpm run registry:build` output committed.

Do NOT run git write commands — the orchestrator owns the working tree (Principle X). Do not touch `.reference/`, `.specify/`, `.claude/`, `scripts/`, `.port-state.json`, or `.port-logs/`.

---

## Phase 8: Convergence

> Appended by `/speckit-converge` after Phase 7. The four quality gates are green at this point
> (`check`: 0 errors, `lint`: clean, `test:unit --run`: 1791 passing, `build`: succeeds), so every
> task below must keep them green — no suppressions, no loosened config.

- [X] T031 **CRITICAL** In `src/lib/components/ui/editable/editable-input.svelte`, merge the part's own `disabled`, `readOnly` and `required` props with the root's by OR (`disabled || root.disabled`, `readOnly || root.readOnly`, `required || root.required`) instead of the current nullish fallback at lines 67–69, matching upstream `editable.tsx:533-535` and plan.md's Public API → `Input` table ("OR-ed with the root's, as upstream does"). As written, `<Editable.Input readOnly={false} />` under a read-only root renders a writable input whose keystrokes `EditableRootState.setText` drops without the `oninput` re-pin ever running, desyncing the DOM from state, and `<Editable.Input disabled={false} />` under a disabled root drops the native `disabled` attribute — an undocumented divergence from upstream, which Constitution II (NON-NEGOTIABLE) forbids. Add the two guard-rail cases to `src/lib/components/ui/editable/editable.test.ts` (own prop `false` + root `true` still yields `readonly` / `disabled` on the rendered input, and typing is still rejected with the DOM value pinned to the state value) per FR-010 and `contracts/data-attributes.md`'s "own prop OR root's" rows (contradicts)
- [X] T032 In `src/lib/components/ui/editable/editable.test.ts`, cover `Tab` as a first-class key per FR-009a, SC-004 and the upstream MDX `KeyboardShortcutsTable`: from an open input, `await user.tab()` onto the `outside` button commits the typed text (`onSubmit` fires, edit mode ends, the preview shows the new value) and the keydown is not intercepted (`Tab` is never `preventDefault()`-ed by the input); using the `'with-trigger'` mode with `triggerForceMount`, tabbing from the input onto the trigger — and, in a separate case, onto `Editable.Cancel` — does **not** submit, exercising the R-15 guard through a real gesture rather than only through `fireEvent.blur({ relatedTarget })` (missing)
- [X] T033 In `src/lib/components/ui/editable/editable.test.ts`, assert the DOM-versus-state contract when an authoritative parent declines a value write (`binding: 'function'`), which research R-12's second claim ("the same resync protects the authoritative function-binding case") does not describe accurately: after typing into the input, the input's DOM value holds the typed text while `state`/the preview stay at the parent's value, and `Editable.Submit` therefore hands `onSubmit` the typed text (`editable-submit.svelte:55` reads `root.inputElement?.value`) — the `oninput` re-pin at `editable-input.svelte:118-121` runs only on the disabled/read-only path by design. Add a one-line comment in the test naming the divergence from R-12's wording so the behaviour is documented where it is proven (partial)
- [X] T034 In `src/routes/docs/components/editable/+page.svelte`, add the missing `children` row to the `rootProps` table (`Snippet`, default `—`, "The composed parts.") so all nine props tables match plan.md's Public API section exactly, as T029 requires and SC-005 promises ("documents every prop of all nine parts") — every other part's table already lists `children` (partial)
